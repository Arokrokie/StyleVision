# Copy this file to `.env` and fill values for local testing or deployment.
SECRET_KEY=replace-with-your-secret
DEBUG=False
ALLOWED_HOSTS=yourapp.azurewebsites.net
# Example Postgres DATABASE_URL (replace with your Azure Database for PostgreSQL connection string)
# DATABASE_URL=postgres://USER:PASS@HOST:PORT/DBNAME
