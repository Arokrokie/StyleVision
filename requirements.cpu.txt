### requirements.cpu.txt
## Mirrors requirements.txt for CPU deployments (SegFormer + Replicate only).
## Install CPU torch/torchvision wheels before this file to avoid CUDA downloads.

# Core web/runtime
Django==4.2.7
gunicorn==20.1.0
whitenoise==6.5.0
django-crispy-forms==2.5
crispy-bootstrap5==2025.6

# Imaging / utilities
Pillow==12.0.0
numpy==2.2.6
opencv-python==4.12.0.88

# ML components actually used
transformers==4.57.2
safetensors==0.7.0
scikit-learn==1.6.1

# Networking / helpers
requests==2.32.5
python-dotenv==1.0.0